#include <cstdio>
#include <algorithm>
using namespace std;

int teskes, n, a[2500], lis[2500], lds[2500];
int main() {
    scanf("%d", &teskes);
    while(teskes--) {
        scanf("%d", &n);
        for(int i = 0; i < n; i++) {
            scanf("%d", &a[i]);
        }
        for(int i = 0; i < n; i++) {
            for(int
        }
    }
    return 0;
}
